# Phase 8A - Creative Library Assignments

Status: implemented locally, awaiting commit decision.

## Product Lead

- Keep the slice focused on repeatable creative work: find, reopen, repeat and reuse.
- Defer favorites and saved styles to a later Phase 8 slice.

## UI UX

- Keep search and filters compact and horizontally scrollable.
- Preserve the image-led two-column project grid.

## Mobile Architecture

- Encode reusable generator drafts in route query parameters.
- Keep provider credentials and generation routing outside the mobile client.

## Mobile Implementation

- Add prompt/model/template search and format/status filters.
- Prefill generator category, model and prompt from a previous job.
- Pass an output asset as input for reuse and upscale flows.

## Backend Data

- Reuse existing backend-shaped job and asset identifiers.
- Keep the current mock/offline repository as the default runtime.

## QA Release

- Cover route contracts, filtering and generator prefill with regression tests.
- Run localization audit, analyzer and the full Flutter test suite.

